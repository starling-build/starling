# A symlink into a sibling checkout, made by ./bootstrap.sh — a machine
# path, not content. (sdk/ used to be its second; the framework now lives
# in-repo.)
/engine
.build/
.build-starling/
.build-gtk/
.DS_Store
.stage/
/build/tools/drm_screenshot
__pycache__/

# Fetched at build time by sdk/tools/fetch-conpty.ps1 (MIT, from nuget.org).
# Binaries are not committed; run the script to populate.
sdk/Vendor/
.build-shared/

# The iOS .app bundle build/ios-app.sh assembles.
.stage-ios/
.glyph-pattern.sh
.glyph-pixels.png
