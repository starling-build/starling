// Copyright the Starling authors
// SPDX-License-Identifier: Apache-2.0

import Flutter

// Rooted in `StarlingApp`, which follows the desktop's appearance and style
// pushes and installs both control families' themes (see the SDK's
// Starling/StarlingApp.swift).
runApp(StarlingApp(title: "Calculator", home: CalculatorApp()))
