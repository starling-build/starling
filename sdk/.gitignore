.build/
.DS_Store
/engine
Package.resolved

# What tools/stage-windows.ps1 assembles: a runnable app directory (exe, the
# Swift runtime DLLs, the engine, its assets). Build output, ~134 MB a copy.
dist/
